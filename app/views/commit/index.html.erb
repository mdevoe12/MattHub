<h1><%= current_user.nickname %>'s Recent Commits</h1>

  <table>
    <thead>
      <tr>
        <th width="250">Repository</th>
        <th scope = "col">Date</th>
      </tr>
    </thead>
    <% @commits.commits.each do |commit| %>
      <tr>
        <td><%= commit.values[0] %></td>
        <td><%= commit.keys[0].to_date %></td>
      </tr>
    <% end %>
  </table>
